# pixifact interactive showcase

This example demonstrates the main pixifact primitives in one scene:

- `Application` and the root `Group`
- `GameObject.instantiate`
- `Component` updates driven by the application ticker
- `Layout`, `GridLayout`, `FlexGroup`, and `Flex`
- Pixi pointer events on pixifact display objects
- DOM-backed `Input` and `Textarea`

Run the example from the repository root:

```bash
bun run example
```

Build it with:

```bash
bun run example:build
```
