# Space HUD Game

完整 Pixifact MVP 示例游戏，用来验证 Editor 的运行按钮、Pixifact runtime 集成和 Agent / CLI 工作流。

```bash
bun run dev
bun run build
```

Pixifact Editor 打开本目录后，可以双击 `scenes/Hud.scene` 预览 HUD，再点击运行启动真实游戏。
